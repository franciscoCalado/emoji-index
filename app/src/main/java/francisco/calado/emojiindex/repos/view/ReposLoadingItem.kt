package francisco.calado.emojiindex.repos.view

import android.view.View

class ReposLoadingItem(itemView: View) : ReposItem(itemView) {
    override fun bind(repo: String) {
        //Loading view holder, nothing to bind
    }
}